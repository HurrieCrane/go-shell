package parser

// Parser struct contains
// information about the
// compiler
type Parser struct {
	Name string
}

// Parse causes a string of text
// to be parsed into
func (p Parser) Parse() int {
	return 0
}
